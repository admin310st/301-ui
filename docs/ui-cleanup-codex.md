# UI Cleanup: Normalize Chips, Buttons & Table Controls

## 📝 Техническое ТЗ для Codex

### Задача

Привести UI компонентов в `301-ui` к единому состоянию согласно `docs/StyleGuide.md`.

### Блоки задачи

---

## 1. Исправить структуру чипов (`btn-chip`)

### Что сделать

1. Ввести внутренние элементы:

* `.btn-chip__icon`
* `.btn-chip__label`
* `.btn-chip__chevron`

2. Обновить разметку во всех демо-страницах:

* `/static/ui-style-guide.html`
* `/static/icons-preview.html`
* таблицы доменов (`domains-table-demo`)

3. Иконки должны использовать структуру:

```html
<span class="btn-chip__icon" data-icon="..."></span>
```

---

## 2. Удалить устаревшие модификаторы кнопок

### Было

* `btn-danger`
* `btn-ghost`

### Стало (как в гайде)

* `btn--danger`
* `btn--ghost`

Обновить разметку + CSS.

---

## 3. Привести кнопки к правильной высоте

Все кнопки и чипы должны использовать токены:

```
--control-height-sm
--control-height-md
--control-height-lg
```

Проверить во всех демках и компонентах таблиц.

---

## 4. Привести первичные кнопки к размерам полей

На данный момент `.btn--primary` слишком высокая.

### Сделать

* высота через `--control-height-md`
* вертикальные паддинги только через токены
* удалить кастомные высоты/line-height

---

## 5. Обновить демку таблицы доменов

### Требования

* исправить чипированные кнопки в хедере таблицы
* все три вида чипов показать на демке:

  1. dropdown chip
  2. provider chip (Cloudflare)
  3. status chip
* показать primary-кнопку в правильном размере рядом с чипами

---

## 6. Обновить документацию

### Файлы

* `docs/StyleGuide.md`
* `docs/ui-roadmap.ru.md`

### Что обновить

* описание структуры чипов
* раздел про кнопки и модификаторы
* токены, применяемые в таблицах
* единые правила высоты компонентов

---

## 7. Обновить README репозитория

Добавить секцию:

```
All UI components must strictly follow docs/StyleGuide.md.
Demo pages are required to always match real components.
```

---

## Ожидаемый результат

* Чипы выглядят одинаково на всех страницах
* Кнопки единообразны
* Таблицы отображают корректные чипы
* Документация синхронизирована с демками
* Репо очищено от старых классов и CSS
